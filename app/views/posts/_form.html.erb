<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-floating mb-2">
    <%= form.text_field :title, class: "form-control", placeholder: "Leave your title here" %>
    <%= form.label :title, for: "floatingTextarea" %>
  </div>

  <div class="form-floating">
    <%= form.text_area :body, style: "height: 100px " ,class: "form-control", placeholder: "Leave a comment here" %>
    <%= form.label :Comments , for: "floatingTextarea"%>
  </div>

  <%
=begin%>
 <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div> 
<%
=end%>

  <div class="mt-3">
    <%= form.submit class: "btn btn-dark" %>
  </div>
<% end %>
